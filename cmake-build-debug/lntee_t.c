/*
 *  This file is auto generated by oeedger8r. DO NOT EDIT.
 */
#include "lntee_t.h"

#include <openenclave/edger8r/enclave.h>

OE_EXTERNC_BEGIN

/**** Trusted function IDs ****/
enum
{
    lntee_fcn_id_ecall_lntee_main = 0,
    lntee_fcn_id_ecall_lntee_init_tee = 1,
    lntee_fcn_id_ecall_lntee_send = 2,
    lntee_fcn_id_ecall_lntee_recv_transaction = 3,
    lntee_fcn_id_ecall_lntee_direct_send = 4,
    lntee_fcn_id_ecall_lntee_direct_recv = 5,
    lntee_fcn_id_ecall_lntee_generate_contract_instance_id = 6,
    lntee_fcn_id_trusted_call_id_max = OE_ENUM_MAX
};

/**** ECALL marshalling structs. ****/
typedef struct _ecall_lntee_main_args_t
{
    oe_result_t _result;
    char* contract_definition;
    size_t contract_definition_len;
    char* address;
    size_t addr_size;
} ecall_lntee_main_args_t;

typedef struct _ecall_lntee_init_tee_args_t
{
    oe_result_t _result;
    int _retval;
    char* seed;
    size_t seed_len;
    char* pubkey;
    char* addr;
} ecall_lntee_init_tee_args_t;

typedef struct _ecall_lntee_send_args_t
{
    oe_result_t _result;
    char* function_call;
    char* tx_str;
} ecall_lntee_send_args_t;

typedef struct _ecall_lntee_recv_transaction_args_t
{
    oe_result_t _result;
    int _retval;
    char* transaction;
    size_t transaction_len;
} ecall_lntee_recv_transaction_args_t;

typedef struct _ecall_lntee_direct_send_args_t
{
    oe_result_t _result;
    char* pubkey;
    size_t pubkey_len;
    int amt;
    char* tx;
} ecall_lntee_direct_send_args_t;

typedef struct _ecall_lntee_direct_recv_args_t
{
    oe_result_t _result;
    int _retval;
    char* tx;
    size_t tx_len;
} ecall_lntee_direct_recv_args_t;

typedef struct _ecall_lntee_generate_contract_instance_id_args_t
{
    oe_result_t _result;
    int _retval;
} ecall_lntee_generate_contract_instance_id_args_t;

/**** ECALL functions. ****/

static void ecall_ecall_lntee_main(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;

    /* Prepare parameters. */
    ecall_lntee_main_args_t* pargs_in = (ecall_lntee_main_args_t*)input_buffer;
    ecall_lntee_main_args_t* pargs_out = (ecall_lntee_main_args_t*)output_buffer;

    size_t input_buffer_offset = 0;
    size_t output_buffer_offset = 0;
    OE_ADD_SIZE(input_buffer_offset, sizeof(*pargs_in));
    OE_ADD_SIZE(output_buffer_offset, sizeof(*pargs_out));

    /* Make sure input and output buffers lie within the enclave. */
    /* oe_is_within_enclave explicitly checks if buffers are null or not. */
    if (!oe_is_within_enclave(input_buffer, input_buffer_size))
        goto done;

    if (!oe_is_within_enclave(output_buffer, output_buffer_size))
        goto done;

    /* Set in and in-out pointers. */
    if (pargs_in->contract_definition)
        OE_SET_IN_POINTER(contract_definition, pargs_in->contract_definition_len * sizeof(char), char*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    if (pargs_in->address)
        OE_SET_OUT_POINTER(address, pargs_in->addr_size, char*);

    /* Check that in/in-out strings are null terminated. */
    OE_CHECK_NULL_TERMINATOR(pargs_in->contract_definition, pargs_in->contract_definition_len);

    /* lfence after checks. */
    oe_lfence();

    /* Call user function. */
    ecall_lntee_main(
        (const char*)pargs_in->contract_definition,
        pargs_in->address,
        pargs_in->addr_size);

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = output_buffer_offset;

done:
    if (output_buffer_size >= sizeof(*pargs_out) &&
        oe_is_within_enclave(pargs_out, output_buffer_size))
        pargs_out->_result = _result;
}

static void ecall_ecall_lntee_init_tee(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;

    /* Prepare parameters. */
    ecall_lntee_init_tee_args_t* pargs_in = (ecall_lntee_init_tee_args_t*)input_buffer;
    ecall_lntee_init_tee_args_t* pargs_out = (ecall_lntee_init_tee_args_t*)output_buffer;

    size_t input_buffer_offset = 0;
    size_t output_buffer_offset = 0;
    OE_ADD_SIZE(input_buffer_offset, sizeof(*pargs_in));
    OE_ADD_SIZE(output_buffer_offset, sizeof(*pargs_out));

    /* Make sure input and output buffers lie within the enclave. */
    /* oe_is_within_enclave explicitly checks if buffers are null or not. */
    if (!oe_is_within_enclave(input_buffer, input_buffer_size))
        goto done;

    if (!oe_is_within_enclave(output_buffer, output_buffer_size))
        goto done;

    /* Set in and in-out pointers. */
    if (pargs_in->seed)
        OE_SET_IN_POINTER(seed, pargs_in->seed_len * sizeof(char), char*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Check that in/in-out strings are null terminated. */
    OE_CHECK_NULL_TERMINATOR(pargs_in->seed, pargs_in->seed_len);

    /* lfence after checks. */
    oe_lfence();

    /* Call user function. */
    pargs_out->_retval = ecall_lntee_init_tee(
        (const char*)pargs_in->seed,
        (const char*)pargs_in->pubkey,
        (const char*)pargs_in->addr);

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = output_buffer_offset;

done:
    if (output_buffer_size >= sizeof(*pargs_out) &&
        oe_is_within_enclave(pargs_out, output_buffer_size))
        pargs_out->_result = _result;
}

static void ecall_ecall_lntee_send(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;

    /* Prepare parameters. */
    ecall_lntee_send_args_t* pargs_in = (ecall_lntee_send_args_t*)input_buffer;
    ecall_lntee_send_args_t* pargs_out = (ecall_lntee_send_args_t*)output_buffer;

    size_t input_buffer_offset = 0;
    size_t output_buffer_offset = 0;
    OE_ADD_SIZE(input_buffer_offset, sizeof(*pargs_in));
    OE_ADD_SIZE(output_buffer_offset, sizeof(*pargs_out));

    /* Make sure input and output buffers lie within the enclave. */
    /* oe_is_within_enclave explicitly checks if buffers are null or not. */
    if (!oe_is_within_enclave(input_buffer, input_buffer_size))
        goto done;

    if (!oe_is_within_enclave(output_buffer, output_buffer_size))
        goto done;

    /* Set in and in-out pointers. */
    /* There were no in nor in-out parameters. */

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Check that in/in-out strings are null terminated. */
    /* There were no in nor in-out string parameters. */

    /* lfence after checks. */
    oe_lfence();

    /* Call user function. */
    ecall_lntee_send(
        (const char*)pargs_in->function_call,
        pargs_in->tx_str);

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = output_buffer_offset;

done:
    if (output_buffer_size >= sizeof(*pargs_out) &&
        oe_is_within_enclave(pargs_out, output_buffer_size))
        pargs_out->_result = _result;
}

static void ecall_ecall_lntee_recv_transaction(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;

    /* Prepare parameters. */
    ecall_lntee_recv_transaction_args_t* pargs_in = (ecall_lntee_recv_transaction_args_t*)input_buffer;
    ecall_lntee_recv_transaction_args_t* pargs_out = (ecall_lntee_recv_transaction_args_t*)output_buffer;

    size_t input_buffer_offset = 0;
    size_t output_buffer_offset = 0;
    OE_ADD_SIZE(input_buffer_offset, sizeof(*pargs_in));
    OE_ADD_SIZE(output_buffer_offset, sizeof(*pargs_out));

    /* Make sure input and output buffers lie within the enclave. */
    /* oe_is_within_enclave explicitly checks if buffers are null or not. */
    if (!oe_is_within_enclave(input_buffer, input_buffer_size))
        goto done;

    if (!oe_is_within_enclave(output_buffer, output_buffer_size))
        goto done;

    /* Set in and in-out pointers. */
    if (pargs_in->transaction)
        OE_SET_IN_POINTER(transaction, pargs_in->transaction_len * sizeof(char), char*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Check that in/in-out strings are null terminated. */
    OE_CHECK_NULL_TERMINATOR(pargs_in->transaction, pargs_in->transaction_len);

    /* lfence after checks. */
    oe_lfence();

    /* Call user function. */
    pargs_out->_retval = ecall_lntee_recv_transaction(
        (const char*)pargs_in->transaction);

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = output_buffer_offset;

done:
    if (output_buffer_size >= sizeof(*pargs_out) &&
        oe_is_within_enclave(pargs_out, output_buffer_size))
        pargs_out->_result = _result;
}

static void ecall_ecall_lntee_direct_send(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;

    /* Prepare parameters. */
    ecall_lntee_direct_send_args_t* pargs_in = (ecall_lntee_direct_send_args_t*)input_buffer;
    ecall_lntee_direct_send_args_t* pargs_out = (ecall_lntee_direct_send_args_t*)output_buffer;

    size_t input_buffer_offset = 0;
    size_t output_buffer_offset = 0;
    OE_ADD_SIZE(input_buffer_offset, sizeof(*pargs_in));
    OE_ADD_SIZE(output_buffer_offset, sizeof(*pargs_out));

    /* Make sure input and output buffers lie within the enclave. */
    /* oe_is_within_enclave explicitly checks if buffers are null or not. */
    if (!oe_is_within_enclave(input_buffer, input_buffer_size))
        goto done;

    if (!oe_is_within_enclave(output_buffer, output_buffer_size))
        goto done;

    /* Set in and in-out pointers. */
    if (pargs_in->pubkey)
        OE_SET_IN_POINTER(pubkey, pargs_in->pubkey_len * sizeof(char), char*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Check that in/in-out strings are null terminated. */
    OE_CHECK_NULL_TERMINATOR(pargs_in->pubkey, pargs_in->pubkey_len);

    /* lfence after checks. */
    oe_lfence();

    /* Call user function. */
    ecall_lntee_direct_send(
        (const char*)pargs_in->pubkey,
        pargs_in->amt,
        pargs_in->tx);

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = output_buffer_offset;

done:
    if (output_buffer_size >= sizeof(*pargs_out) &&
        oe_is_within_enclave(pargs_out, output_buffer_size))
        pargs_out->_result = _result;
}

static void ecall_ecall_lntee_direct_recv(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;

    /* Prepare parameters. */
    ecall_lntee_direct_recv_args_t* pargs_in = (ecall_lntee_direct_recv_args_t*)input_buffer;
    ecall_lntee_direct_recv_args_t* pargs_out = (ecall_lntee_direct_recv_args_t*)output_buffer;

    size_t input_buffer_offset = 0;
    size_t output_buffer_offset = 0;
    OE_ADD_SIZE(input_buffer_offset, sizeof(*pargs_in));
    OE_ADD_SIZE(output_buffer_offset, sizeof(*pargs_out));

    /* Make sure input and output buffers lie within the enclave. */
    /* oe_is_within_enclave explicitly checks if buffers are null or not. */
    if (!oe_is_within_enclave(input_buffer, input_buffer_size))
        goto done;

    if (!oe_is_within_enclave(output_buffer, output_buffer_size))
        goto done;

    /* Set in and in-out pointers. */
    if (pargs_in->tx)
        OE_SET_IN_POINTER(tx, pargs_in->tx_len * sizeof(char), char*);

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Check that in/in-out strings are null terminated. */
    OE_CHECK_NULL_TERMINATOR(pargs_in->tx, pargs_in->tx_len);

    /* lfence after checks. */
    oe_lfence();

    /* Call user function. */
    pargs_out->_retval = ecall_lntee_direct_recv(
        (const char*)pargs_in->tx);

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = output_buffer_offset;

done:
    if (output_buffer_size >= sizeof(*pargs_out) &&
        oe_is_within_enclave(pargs_out, output_buffer_size))
        pargs_out->_result = _result;
}

static void ecall_ecall_lntee_generate_contract_instance_id(
    uint8_t* input_buffer,
    size_t input_buffer_size,
    uint8_t* output_buffer,
    size_t output_buffer_size,
    size_t* output_bytes_written)
{
    oe_result_t _result = OE_FAILURE;

    /* Prepare parameters. */
    ecall_lntee_generate_contract_instance_id_args_t* pargs_in = (ecall_lntee_generate_contract_instance_id_args_t*)input_buffer;
    ecall_lntee_generate_contract_instance_id_args_t* pargs_out = (ecall_lntee_generate_contract_instance_id_args_t*)output_buffer;

    size_t input_buffer_offset = 0;
    size_t output_buffer_offset = 0;
    OE_ADD_SIZE(input_buffer_offset, sizeof(*pargs_in));
    OE_ADD_SIZE(output_buffer_offset, sizeof(*pargs_out));

    /* Make sure input and output buffers lie within the enclave. */
    /* oe_is_within_enclave explicitly checks if buffers are null or not. */
    if (!oe_is_within_enclave(input_buffer, input_buffer_size))
        goto done;

    if (!oe_is_within_enclave(output_buffer, output_buffer_size))
        goto done;

    /* Set in and in-out pointers. */
    /* There were no in nor in-out parameters. */

    /* Set out and in-out pointers. */
    /* In-out parameters are copied to output buffer. */
    /* There were no out nor in-out parameters. */

    /* Check that in/in-out strings are null terminated. */
    /* There were no in nor in-out string parameters. */

    /* lfence after checks. */
    oe_lfence();

    /* Call user function. */
    pargs_out->_retval = ecall_lntee_generate_contract_instance_id(
    );

    /* Success. */
    _result = OE_OK;
    *output_bytes_written = output_buffer_offset;

done:
    if (output_buffer_size >= sizeof(*pargs_out) &&
        oe_is_within_enclave(pargs_out, output_buffer_size))
        pargs_out->_result = _result;
}

/**** ECALL function table. ****/

oe_ecall_func_t __oe_ecalls_table[] = {
    (oe_ecall_func_t) ecall_ecall_lntee_main,
    (oe_ecall_func_t) ecall_ecall_lntee_init_tee,
    (oe_ecall_func_t) ecall_ecall_lntee_send,
    (oe_ecall_func_t) ecall_ecall_lntee_recv_transaction,
    (oe_ecall_func_t) ecall_ecall_lntee_direct_send,
    (oe_ecall_func_t) ecall_ecall_lntee_direct_recv,
    (oe_ecall_func_t) ecall_ecall_lntee_generate_contract_instance_id
};

size_t __oe_ecalls_table_size = OE_COUNTOF(__oe_ecalls_table);

/**** Untrusted function IDs. ****/
enum
{
    lntee_fcn_id_ocall_lntee_broadcast = 0,
    lntee_fcn_id_ocall_lntee_send_transaction = 1,
    lntee_fcn_id_ocall_lntee_time_log = 2,
    lntee_fcn_id_ocall_lntee_pubkey = 3,
    lntee_fcn_id_untrusted_call_max = OE_ENUM_MAX
};

/**** OCALL marshalling structs. ****/
typedef struct _ocall_lntee_broadcast_args_t
{
    oe_result_t _result;
    char* tx;
    size_t tx_len;
    char* instance_id;
    size_t instance_id_len;
} ocall_lntee_broadcast_args_t;

typedef struct _ocall_lntee_send_transaction_args_t
{
    oe_result_t _result;
    char* target_pubkey;
    size_t target_pubkey_len;
    char* tx;
    size_t tx_len;
} ocall_lntee_send_transaction_args_t;

typedef struct _ocall_lntee_time_log_args_t
{
    oe_result_t _result;
} ocall_lntee_time_log_args_t;

typedef struct _ocall_lntee_pubkey_args_t
{
    oe_result_t _result;
    char* pubkey;
    size_t pubkey_len;
    size_t len;
} ocall_lntee_pubkey_args_t;

/**** OCALL function wrappers. ****/

oe_result_t ocall_lntee_broadcast(
    const char* tx,
    const char* instance_id)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    ocall_lntee_broadcast_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* No pointers to save for deep copy. */

    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.tx = (char*)tx;
    _args.tx_len = (tx) ? (oe_strlen(tx) + 1) : 0;
    _args.instance_id = (char*)instance_id;
    _args.instance_id_len = (instance_id) ? (oe_strlen(instance_id) + 1) : 0;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(ocall_lntee_broadcast_args_t));
    if (tx)
        OE_ADD_SIZE(_input_buffer_size, _args.tx_len * sizeof(char));
    if (instance_id)
        OE_ADD_SIZE(_input_buffer_size, _args.instance_id_len * sizeof(char));
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(ocall_lntee_broadcast_args_t));
    /* There were no corresponding parameters. */
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (ocall_lntee_broadcast_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (tx)
        OE_WRITE_IN_PARAM(tx, _args.tx_len * sizeof(char), char*);
    if (instance_id)
        OE_WRITE_IN_PARAM(instance_id, _args.instance_id_len * sizeof(char), char*);
    
    /* Copy args structure (now filled) to input buffer. */
    memcpy(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             lntee_fcn_id_ocall_lntee_broadcast,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Setup output arg struct pointer. */
    _pargs_out = (ocall_lntee_broadcast_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));
    
    /* Check if the call succeeded. */
    if ((_result = _pargs_out->_result) != OE_OK)
        goto done;
    
    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }
    
    /* Unmarshal return value and out, in-out parameters. */
    /* No return value. */
    /* No pointers to restore for deep copy. */
    /* There were no out nor in-out parameters. */

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);
    return _result;
}

oe_result_t ocall_lntee_send_transaction(
    const char* target_pubkey,
    const char* tx)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    ocall_lntee_send_transaction_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* No pointers to save for deep copy. */

    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.target_pubkey = (char*)target_pubkey;
    _args.target_pubkey_len = (target_pubkey) ? (oe_strlen(target_pubkey) + 1) : 0;
    _args.tx = (char*)tx;
    _args.tx_len = (tx) ? (oe_strlen(tx) + 1) : 0;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(ocall_lntee_send_transaction_args_t));
    if (target_pubkey)
        OE_ADD_SIZE(_input_buffer_size, _args.target_pubkey_len * sizeof(char));
    if (tx)
        OE_ADD_SIZE(_input_buffer_size, _args.tx_len * sizeof(char));
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(ocall_lntee_send_transaction_args_t));
    /* There were no corresponding parameters. */
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (ocall_lntee_send_transaction_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (target_pubkey)
        OE_WRITE_IN_PARAM(target_pubkey, _args.target_pubkey_len * sizeof(char), char*);
    if (tx)
        OE_WRITE_IN_PARAM(tx, _args.tx_len * sizeof(char), char*);
    
    /* Copy args structure (now filled) to input buffer. */
    memcpy(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             lntee_fcn_id_ocall_lntee_send_transaction,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Setup output arg struct pointer. */
    _pargs_out = (ocall_lntee_send_transaction_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));
    
    /* Check if the call succeeded. */
    if ((_result = _pargs_out->_result) != OE_OK)
        goto done;
    
    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }
    
    /* Unmarshal return value and out, in-out parameters. */
    /* No return value. */
    /* No pointers to restore for deep copy. */
    /* There were no out nor in-out parameters. */

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);
    return _result;
}

oe_result_t ocall_lntee_time_log(
    )
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    ocall_lntee_time_log_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* No pointers to save for deep copy. */

    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(ocall_lntee_time_log_args_t));
    /* There were no corresponding parameters. */
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(ocall_lntee_time_log_args_t));
    /* There were no corresponding parameters. */
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (ocall_lntee_time_log_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    /* There were no in nor in-out parameters. */
    
    /* Copy args structure (now filled) to input buffer. */
    memcpy(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             lntee_fcn_id_ocall_lntee_time_log,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Setup output arg struct pointer. */
    _pargs_out = (ocall_lntee_time_log_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));
    
    /* Check if the call succeeded. */
    if ((_result = _pargs_out->_result) != OE_OK)
        goto done;
    
    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }
    
    /* Unmarshal return value and out, in-out parameters. */
    /* No return value. */
    /* No pointers to restore for deep copy. */
    /* There were no out nor in-out parameters. */

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);
    return _result;
}

oe_result_t ocall_lntee_pubkey(
    const char* pubkey,
    size_t len)
{
    oe_result_t _result = OE_FAILURE;

    /* If the enclave is in crashing/crashed status, new OCALL should fail
       immediately. */
    if (oe_get_enclave_status() != OE_OK)
        return oe_get_enclave_status();

    /* Marshalling struct. */
    ocall_lntee_pubkey_args_t _args, *_pargs_in = NULL, *_pargs_out = NULL;
    /* No pointers to save for deep copy. */

    /* Marshalling buffer and sizes. */
    size_t _input_buffer_size = 0;
    size_t _output_buffer_size = 0;
    size_t _total_buffer_size = 0;
    uint8_t* _buffer = NULL;
    uint8_t* _input_buffer = NULL;
    uint8_t* _output_buffer = NULL;
    size_t _input_buffer_offset = 0;
    size_t _output_buffer_offset = 0;
    size_t _output_bytes_written = 0;

    /* Fill marshalling struct. */
    memset(&_args, 0, sizeof(_args));
    _args.pubkey = (char*)pubkey;
    _args.pubkey_len = (pubkey) ? (oe_strlen(pubkey) + 1) : 0;
    _args.len = len;

    /* Compute input buffer size. Include in and in-out parameters. */
    OE_ADD_SIZE(_input_buffer_size, sizeof(ocall_lntee_pubkey_args_t));
    if (pubkey)
        OE_ADD_SIZE(_input_buffer_size, _args.pubkey_len * sizeof(char));
    
    /* Compute output buffer size. Include out and in-out parameters. */
    OE_ADD_SIZE(_output_buffer_size, sizeof(ocall_lntee_pubkey_args_t));
    /* There were no corresponding parameters. */
    
    /* Allocate marshalling buffer. */
    _total_buffer_size = _input_buffer_size;
    OE_ADD_SIZE(_total_buffer_size, _output_buffer_size);
    _buffer = (uint8_t*)oe_allocate_ocall_buffer(_total_buffer_size);
    _input_buffer = _buffer;
    _output_buffer = _buffer + _input_buffer_size;
    if (_buffer == NULL)
    {
        _result = OE_OUT_OF_MEMORY;
        goto done;
    }
    
    /* Serialize buffer inputs (in and in-out parameters). */
    _pargs_in = (ocall_lntee_pubkey_args_t*)_input_buffer;
    OE_ADD_SIZE(_input_buffer_offset, sizeof(*_pargs_in));
    if (pubkey)
        OE_WRITE_IN_PARAM(pubkey, _args.pubkey_len * sizeof(char), char*);
    
    /* Copy args structure (now filled) to input buffer. */
    memcpy(_pargs_in, &_args, sizeof(*_pargs_in));

    /* Call host function. */
    if ((_result = oe_call_host_function(
             lntee_fcn_id_ocall_lntee_pubkey,
             _input_buffer,
             _input_buffer_size,
             _output_buffer,
             _output_buffer_size,
             &_output_bytes_written)) != OE_OK)
        goto done;

    /* Setup output arg struct pointer. */
    _pargs_out = (ocall_lntee_pubkey_args_t*)_output_buffer;
    OE_ADD_SIZE(_output_buffer_offset, sizeof(*_pargs_out));
    
    /* Check if the call succeeded. */
    if ((_result = _pargs_out->_result) != OE_OK)
        goto done;
    
    /* Currently exactly _output_buffer_size bytes must be written. */
    if (_output_bytes_written != _output_buffer_size)
    {
        _result = OE_FAILURE;
        goto done;
    }
    
    /* Unmarshal return value and out, in-out parameters. */
    /* No return value. */
    /* No pointers to restore for deep copy. */
    /* There were no out nor in-out parameters. */

    /* Retrieve propagated errno from OCALL. */
    /* Errno propagation not enabled. */

    _result = OE_OK;

done:
    if (_buffer)
        oe_free_ocall_buffer(_buffer);
    return _result;
}

OE_EXTERNC_END
